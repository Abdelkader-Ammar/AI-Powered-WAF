/* Static assets for the AI-WAF Python dashboard.
   HTMX is loaded from a CDN in base.html; vendor it here for offline use:
   curl -L https://unpkg.com/htmx.org@2.0.3 -o static/htmx.min.js
   then change the <script src> in base.html to /static/htmx.min.js */
