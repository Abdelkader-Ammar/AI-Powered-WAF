# Copy to .env and adjust. Demo-only values.
WAF_URL=http://localhost:8080
REDIS_HOST=localhost
REDIS_PORT=6379
# Tier 1 is optional; point at a trained checkpoint to enable it:
# ROBERTA_MODEL_PATH=/models/waf_roberta
